<resources>
    <!--
        Android 15 (API 35) enforces edge-to-edge for apps targeting SDK 35, which
        draws content behind the status/navigation bars. This app uses fixed
        full-screen game layouts that are not inset-aware, so we opt out of the
        automatic enforcement to keep the existing layouts correct. This override
        only applies on API 35+ devices; older devices are unaffected.
    -->
    <style name="Theme.Trivia2" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>
</resources>
